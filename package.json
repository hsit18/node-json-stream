{
  "name": "eyeota-fullstack-project",
  "version": "1.0.0",
  "description": "## The challenge The objective is to build a single interactive page to display a list of top 10 voted `Posts` and their associated `Comments`.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "tsc": "tsc",
    "build": "tsc --project './tsconfig.json'",
    "start": "NODE_ENV=development nodemon --watch dist dist/server/index.js",
    "dev": "tsc --project './tsconfig.json' --watch & NODE_ENV=development nodemon --watch dist dist/server/index.js"
  },
  "author": "Harpreet Singh",
  "license": "ISC",
  "devDependencies": {
    "@types/body-parser": "^1.19.0",
    "@types/express": "^4.17.6",
    "@types/node": "^13.11.1",
    "nodemon": "^2.0.3",
    "typescript": "^3.8.3"
  },
  "dependencies": {
    "JSONStream": "^1.3.5",
    "body-parser": "^1.19.0",
    "express": "^4.17.1"
  }
}
